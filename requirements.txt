# ── Core scientific stack ─────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.13.0
pandas==2.2.2
scikit-learn==1.4.2

# ── Astronomy / NASA data access ──────────────────────────────────────────────
lightkurve==2.4.2          # official NASA/STScI library for Kepler & TESS light curves
astropy==6.1.0             # FITS file handling, units, time systems
astroquery==0.4.7          # programmatic access to MAST, Simbad, NASA Exoplanet Archive

# ── Deep learning ─────────────────────────────────────────────────────────────
torch==2.3.0               # CPU build is default; Colab will use CUDA automatically
torchvision==0.18.0

# ── Uncertainty quantification ────────────────────────────────────────────────
uncertainty-calibration==0.1.4   # ECE, reliability diagrams, temperature scaling
crepes==0.6.0                    # conformal prediction sets & intervals

# ── Explainability ────────────────────────────────────────────────────────────
shap==0.45.1
captum==0.7.0              # PyTorch-native attribution: saliency, integrated gradients

# ── Experiment tracking ───────────────────────────────────────────────────────
mlflow==2.13.0             # local experiment tracking, no account needed

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0             # interactive plots for notebooks

# ── Notebook / dev tooling ────────────────────────────────────────────────────
jupyterlab==4.5.7
ipywidgets==8.1.3
tqdm==4.66.4
pyyaml==6.0.1
python-dotenv==1.0.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0